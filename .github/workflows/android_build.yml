name: 🚨 FORJA-APK-OTTO-TASK 🚨
on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  apk:
    runs-on: ubuntu-latest
    steps:
      - name: 🛰️ Sincronizando Rama Master
        uses: actions/checkout@v4

      - name: 🛠️ Reparando SSL y Dependencias Android
        run: |
          sudo apt update
          sudo apt install -y git zip unzip openjdk-17-jdk autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev cmake libffi-dev libssl-dev
          pip3 install --upgrade pip
          pip3 install --upgrade certifi  # <--- Esto arregla el error SSL de ayer
          pip3 install --upgrade Cython==0.29.33 virtualenv buildozer

      - name: 🤖 Fabricando APK (Buildozer)
        run: |
          # Esto firma las licencias de Android automáticamente
          yes | sdkmanager --licenses || true
          buildozer android debug

      - name: 📦 Entrega de Otto-Task APK
        uses: actions/upload-artifact@v4
        with:
          name: Otto-Task-Android
          path: bin/*.apk
          
