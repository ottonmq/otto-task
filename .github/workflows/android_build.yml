name: 🚨 FORJA-FINAL-OTTO 🚨
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🛰️ Sincronizando
        uses: actions/checkout@v4

      - name: 🛠️ Instalando Herramientas de Poder
        run: |
          sudo apt update
          sudo apt install -y git zip unzip openjdk-17-jdk autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev cmake libffi-dev libssl-dev
          pip3 install --upgrade pip certifi Cython==0.29.33 virtualenv buildozer

      - name: 🖋️ Aceptando Contratos de Google
        run: |
          mkdir -p ~/.android && touch ~/.android/repositories.cfg
          yes | sdkmanager --licenses || true
          # Forzamos la descarga de lo que le faltaba ayer
          yes | sdkmanager "build-tools;34.0.0" "platform-tools" "platforms;android-34" || true

      - name: 🤖 Fabricando APK (Buildozer)
        run: |
          buildozer android debug

      - name: 📦 Entrega del Botín
        uses: actions/upload-artifact@v4
        with:
          name: Otto-Task-APK
          path: bin/*.apk
          
