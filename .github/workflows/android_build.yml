name: 🚨 FORJA-ANDROID-SOLO 🚨
on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  apk:
    runs-on: ubuntu-latest
    steps:
      - name: 🛰️ Sincronizando Rama Master
        uses: actions/checkout@v4

      - name: 🛠️ Preparando Entorno y Licencias
        run: |
          sudo apt update
          sudo apt install -y git zip unzip openjdk-17-jdk autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev cmake libffi-dev libssl-dev
          pip3 install --upgrade Cython==0.29.33 virtualenv buildozer
          # Aceptar licencias de Android automáticamente
          mkdir -p ~/.android && touch ~/.android/repositories.cfg
          yes | sdkmanager --licenses || true

      - name: 🤖 Forjando APK (Buildozer)
        run: |
          sudo chown -R $USER:$USER .
          buildozer android debug

      - name: 📦 Entrega de Otto-Task
        uses: actions/upload-artifact@v4
        with:
          name: Otto-APK-Final
          path: bin/*.apk
          
