name: 🚨 FORJA-FINAL-OTTO 🚨
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🛰️ Sincronizando
        uses: actions/checkout@v4

      - name: 🛠️ Instalando Herramientas
        run: |
          sudo apt update
          sudo apt install -y git zip unzip openjdk-17-jdk autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev cmake libffi-dev libssl-dev
          pip3 install --upgrade pip certifi Cython==0.29.33 virtualenv buildozer

      - name: 🖋️ Licencias y Aidl
        run: |
          mkdir -p ~/.android && touch ~/.android/repositories.cfg
          yes | sdkmanager --licenses || true
          yes | sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0" || true

      - name: 🤖 Limpieza y Forja (Aquí está el truco)
        run: |
          # Esto borra la basura vieja en GitHub para que no falle
          rm -rf .buildozer/ 
          # Ahora sí, fabricamos desde cero
          buildozer android debug

      - name: 📦 Entrega del APK
        uses: actions/upload-artifact@v4
        with:
          name: Otto-Task-APK
          path: bin/*.apk
          
