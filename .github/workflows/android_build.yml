name: 🚨 FORJA-OTTO-TASK-DEFINITIVA 🚨

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  apk:
    runs-on: ubuntu-latest
    steps:
      - name: 🛰️ Sincronizando Código de Otto-task
        uses: actions/checkout@v4

      - name: 🛠️ Instalando Motores (Java y Herramientas)
        run: |
          sudo apt update
          sudo apt install -y git zip unzip openjdk-17-jdk autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev cmake libffi-dev libssl-dev wget
          pip3 install --upgrade pip certifi Cython==0.29.33 virtualenv buildozer

      - name: 🏗️ Descarga Manual del SDK (Anti-Error 127)
        run: |
          mkdir -p $HOME/android-sdk/cmdline-tools
          wget https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip commandlinetools-linux-9477386_latest.zip -d $HOME/android-sdk/cmdline-tools
          mv $HOME/android-sdk/cmdline-tools/cmdline-tools $HOME/android-sdk/cmdline-tools/latest

      - name: 🖋️ Firmando Licencias de Google
        run: |
          # Definimos la ruta de las herramientas para este paso
          export PATH=$PATH:$HOME/android-sdk/cmdline-tools/latest/bin
          export ANDROID_HOME=$HOME/android-sdk
          yes | sdkmanager --licenses --sdk_root=$ANDROID_HOME || true
          sdkmanager "platforms;android-34" "build-tools;34.0.0" "platform-tools" --sdk_root=$ANDROID_HOME

      - name: 🤖 Forjando el APK Nativo
        run: |
          # Limpieza profunda de archivos fantasma
          rm -rf .buildozer/
          # Inyectamos las rutas de nuevo para que Buildozer sepa dónde está el martillo
          export PATH=$PATH:$HOME/android-sdk/cmdline-tools/latest/bin
          export ANDROID_HOME=$HOME/android-sdk
          # Iniciamos la construcción con el mapa forzado
          buildozer android debug

      - name: 📦 Entrega del Botín (APK)
        uses: actions/upload-artifact@v4
        with:
          name: Otto-Task-Cyber-APK
          path: bin/*.apk
          
