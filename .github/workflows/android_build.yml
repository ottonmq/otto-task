name: 🚨 OTTO-TASK-FINAL-STABLE 🚨

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: 🛰️ Sincronizando Código
        uses: actions/checkout@v4

      - name: 🛠️ Motores y Dependencias
        run: |
          sudo apt update
          sudo apt install -y git zip unzip openjdk-17-jdk autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev cmake libffi-dev libssl-dev wget
          pip3 install --upgrade pip Cython==0.29.33 virtualenv buildozer

      - name: 🏗️ Setup SDK Blindado (Ruta Fija)
        run: |
          mkdir -p $HOME/android-sdk/cmdline-tools
          wget https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip commandlinetools-linux-9477386_latest.zip -d $HOME/android-sdk/cmdline-tools
          mv $HOME/android-sdk/cmdline-tools/cmdline-tools $HOME/android-sdk/cmdline-tools/latest
          # Definimos las variables de entorno para todo el sistema
          echo "ANDROID_HOME=$HOME/android-sdk" >> $GITHUB_ENV
          echo "ANDROID_SDK_ROOT=$HOME/android-sdk" >> $GITHUB_ENV
          echo "$HOME/android-sdk/cmdline-tools/latest/bin" >> $GITHUB_PATH

      - name: 🖋️ Licencias y Parche Aidl (Estable 31)
        run: |
          # Aceptamos licencias globales
          yes | $HOME/android-sdk/cmdline-tools/latest/bin/sdkmanager --sdk_root=$HOME/android-sdk --licenses || true
          # Instalamos la 31 y platform-tools donde está el Aidl real
          $HOME/android-sdk/cmdline-tools/latest/bin/sdkmanager --sdk_root=$HOME/android-sdk "platforms;android-31" "build-tools;31.0.0" "platform-tools"

      - name: 🤖 FORJA NATIVA (Sin errores de Aidl)
        run: |
          # Limpieza obligatoria de intentos fallidos
          rm -rf .buildozer/ || true
          rm -rf bin/ || true
          
          # FORZAMOS A BUILDOZER A USAR NUESTRO SDK (Aquí es donde fallaba)
          export ANDROID_HOME=$HOME/android-sdk
          export ANDROID_SDK_ROOT=$HOME/android-sdk
          export PATH=$PATH:$HOME/android-sdk/cmdline-tools/latest/bin
          
          # EJECUCIÓN CON SALTO DE LICENCIAS INTERNO
          buildozer --verbose android debug --auto-accept-sdk-license || exit 1

      - name: 📦 OBTENER EL BOTÍN (APK)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Otto-Task-APK-Final
          path: bin/*.apk
          
